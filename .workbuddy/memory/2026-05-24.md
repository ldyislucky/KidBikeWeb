# 2026-05-24

## 修正 product-update 图片上传逻辑（最终版）

- **正确逻辑已确认（三步走）**：
  1. `PUT /api/v1/products/{id}` — 保存产品文字信息
  2. `api.uploadFile(filePath)` → `POST /api/v1/files/upload` — 逐张上传图片，拿到 file_name
  3. `api.addProductImages(id, fileNames)` → `POST /api/v1/products/{id}/images` — JSON `{ fileNames: [...] }` 关联图片

- **product-update.js** `_uploadLocalImages`：逐张调用 `api.uploadFile`，收集返回的 `file_name`，resolve 回 `fileNames` 数组
- **product-update.js** `_submitWithImages`：完整三步链式调用（updateProduct → _uploadLocalImages → addProductImages）
- **api.js** 保留 `addProductImages`（JSON 方式传 fileNames），移除之前误加的 `uploadProductImage`（multipart 方式）
