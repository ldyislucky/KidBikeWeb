# 2026-05-22

## 完善微信小程序各功能页面

**完成内容：**

1. **登录守卫**（orders / favorites / wallet / addresses）
   - 每个页面 `onShow` 时调用 `_checkLogin()`，未登录弹 Modal 引导跳 `/pages/login/login`

2. **修复 onPullDownRefresh Promise 链**
   - 所有 fetch 方法改为 `return api.xxx().then().catch()`，确保 stopPullDownRefresh 正常调用

3. **product-detail 未登录引导**
   - 点击收藏/下单未登录时由 Toast 改为 showModal 引导到登录页

4. **support 页一键拨号**
   - 客服电话区域点击触发 `wx.makePhoneCall()`
   - 接口失败时展示默认电话 `400-000-0000` 和工作时间
   - 新增 `.call-badge` 样式（拨打小标签）

5. **app.js autoLogin 完善**
   - 自动登录失败时从本地缓存恢复 userInfo
   - 新增全局 `logout()` 方法，统一清理 token + userInfo + globalData

6. **my.js onLogout 优化**
   - 改为调用 `app.logout()` 统一处理，保持逻辑一致

## 提取认证接口文档供后端开发

- 生成文档：`docs/auth-api.md`
- 涵盖接口：手机号密码登录、微信一键登录、注册账号、获取当前用户信息
- 包含：请求/响应字段、前端字段读取优先级、错误码规范、Token 说明
