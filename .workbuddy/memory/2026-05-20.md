# 2026-05-20

## 简化 my 页未登录交互

- 移除双按钮（立即登录 + 注册账号），改为**整块头像+昵称区域可点击**跳转登录页
- 登录页内有「立即注册」入口跳转注册页，形成完整闭环
- 未登录时显示"点击登录"文字 + "登录后享受完整服务 ›"副标题引导

## 完善登录注册功能

**任务：** 完善 KidBikeWeb 微信小程序的登录注册功能

**完成内容：**

1. **新建 `pages/login/` 登录页面**（4个文件）
   - 支持手机号 + 密码登录（调用 `api.login()`）
   - 支持微信一键登录（调用 `wx.login()` + `api.wechatLogin()`）
   - 密码显示/隐藏切换（👁 图标）
   - 登录成功后返回上级页面或跳转"我的"页
   - 跳转注册页入口

2. **修复 `pages/register/register.js`**
   - `onGoLogin()` 从 `navigateBack()` 改为：有上级页则 back，否则 redirectTo 登录页

3. **完善 `pages/my/my.js` + `my.wxml` + `my.wxss`**
   - 未登录时：展示「立即登录」+「注册账号」双按钮，点击头像也可跳登录
   - 未登录触发需登录菜单：弹 showModal 引导跳登录页（代替 Toast）
   - 新增 `onLogout()` 退出登录：弹确认框，清除 token + userInfo，重置页面状态
   - 已登录时显示「退出登录」按钮

4. **更新 `app.json`**
   - 注册 `pages/login/login` 路由
